
const SearchBar = (props) => {
    return (
        <div>
            <input type="text" />
            <button type='submit' value="Submit">Search</button>
        </div>
    )
}

export default SearchBar;
